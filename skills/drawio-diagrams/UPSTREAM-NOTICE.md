# Upstream notice

The implementation toolkit in this skill is derived from
`Agents365-ai/drawio-skill` version 1.28.0, which declares the MIT license.
The upstream project name and homepage remain in `SKILL.md` metadata for
attribution.

The bundled shape index has separate provenance and licensing notes. See
`data/SHAPE-INDEX-NOTICE.md`.

Team-specific changes merge the implementation into the externally visible
`drawio-diagrams` skill, add diagram-selection and authoring guidance, and
rename operational paths so no second draw.io skill is required.
